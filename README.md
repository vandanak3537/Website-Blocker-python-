# Website-Blocker-python-
Here's a shorter version (around **250 characters**):  > **Built a Python Website Blocker that restricts access to distracting websites by modifying the Windows hosts file. Used Python's `datetime` and file handling to automate blocking and unblocking based on specified working hours.**
